export enum ChainId {
  MAINNET = 1,
  GOERLI = 5,
}

export enum WALLET_TYPES {
  METAMASK = 'metamask',
}

export enum TransactionStatusEnum {
  PENDING = 'pending',
  SUCCESS = 'success',
  ERROR = 'error',
}
